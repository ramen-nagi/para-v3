# Para v3

## Local setup

Copy `.env.example` to `.env` and supply the Supabase publishable key, Supabase
URL, Mapbox token, and Google Maps Platform key. Never put a Supabase secret or
service-role key in the Flutter environment file.

